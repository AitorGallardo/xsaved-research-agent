{
  "name": "xsaved-research-agent",
  "version": "0.1.0",
  "description": "Autonomous research agent over a Twitter/X bookmark corpus: a hand-rolled Claude tool-use loop that drives the xsaved-mcp server (→ xsaved-rag) to produce cited reports",
  "type": "module",
  "scripts": {
    "ask": "tsx src/cli.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "chalk": "^5.6.2",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
